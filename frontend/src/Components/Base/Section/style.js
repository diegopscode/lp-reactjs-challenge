import styled from 'styled-components';

export const Wrapper = styled.div`
  position: relative;
  padding: 40px 0;
`;

export const Title = styled.h3`
  font-weight: normal;
  font-size: 16px;
  line-height: 24px;

  margin-bottom: 10px;
`;
