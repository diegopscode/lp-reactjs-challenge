const {
  products,
  product,
  searchProducts,
  lastVisitedProducts,
  mostVisitedProducts,
} = require('../../functions/products');

module.exports = {
  Query: {
    products,
    product,
    searchProducts,
    lastVisitedProducts,
    mostVisitedProducts,
  },
};
